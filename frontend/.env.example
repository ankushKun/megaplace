# MegaETH WebSocket Configuration
# =================================

# Optional: Alchemy WebSocket URL for MegaETH Testnet
# If provided, this will be used as the primary WebSocket connection
# Get your API key from: https://www.alchemy.com/
# Example: VITE_ALCHEMY_WS_URL=wss://megaeth-testnet.g.alchemy.com/v2/YOUR_API_KEY
VITE_ALCHEMY_WS_URL=

# Note: The app will automatically fallback through these transports:
# 1. Alchemy WebSocket (if VITE_ALCHEMY_WS_URL is set)
# 2. MegaETH WebSocket (wss://timothy.megaeth.com/rpc)
# 3. MegaETH HTTP (https://timothy.megaeth.com/rpc)
